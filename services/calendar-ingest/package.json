{
  "name": "@tv/calendar-ingest",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "main": "./src/index.ts",
  "scripts": {
    "dev": "bun run --watch src/index.ts --once",
    "start": "bun run src/index.ts",
    "once": "bun run src/index.ts --once",
    "lint": "echo skip",
    "typecheck": "tsc --noEmit",
    "test": "bun test --pass-with-no-tests",
    "build": "echo skip"
  },
  "dependencies": {
    "@tv/calendar": "workspace:*",
    "@tv/core": "workspace:*",
    "@tv/db": "workspace:*",
    "drizzle-orm": "^0.36.4"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "typescript": "^5.7.2"
  }
}
