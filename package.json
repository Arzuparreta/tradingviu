{
  "name": "tradingviu",
  "version": "0.1.0",
  "private": true,
  "description": "Personal single-owner market terminal.",
  "engines": {
    "node": ">=22.0.0",
    "bun": ">=1.3.0",
    "pnpm": ">=10.0.0"
  },
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "dev:up": "node tools/dev-server.mjs start",
    "dev:down": "node tools/dev-server.mjs stop",
    "dev:restart": "node tools/dev-server.mjs restart",
    "dev:status": "node tools/dev-server.mjs status",
    "dev:infra": "docker compose --env-file .env -f infra/docker-compose.yml up -d postgres redis minio meilisearch mailpit",
    "dev:infra:down": "docker compose --env-file .env -f infra/docker-compose.yml stop postgres redis minio meilisearch mailpit",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "e2e": "playwright test",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md,yml,yaml}\" --ignore-path .gitignore",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,json,md,yml,yaml}\" --ignore-path .gitignore",
    "db:generate": "pnpm --filter @tv/db generate",
    "db:migrate": "pnpm --filter @tv/db migrate",
    "db:seed": "pnpm --filter @tv/db seed && pnpm --filter @tv/cli tvctl ensure-owner",
    "news:ingest": "bun run services/news-ingest/src/index.ts --once",
    "fundamentals:ingest": "bun run services/fundamentals-ingest/src/index.ts --once",
    "macro:ingest": "bun run services/macro-ingest/src/index.ts --once",
    "calendars:ingest": "bun run services/calendar-ingest/src/index.ts --once",
    "search:reindex": "bun run apps/server/src/reindex-symbols.ts",
    "backfill:bars": "pnpm --filter @tv/backfill-bars backfill:bars",
    "tvctl": "pnpm --filter @tv/cli tvctl"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^22.10.0",
    "prettier": "^3.4.2",
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  }
}
